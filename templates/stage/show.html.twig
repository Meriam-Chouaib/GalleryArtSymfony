{% extends 'base.html.twig' %}

{% block title %} <title>Gallery Art - Stage</title> {% endblock %}

{% block body %}
    <h1>Stage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stage.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ stage.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stage_index') }}">back to list</a>

    <a href="{{ path('stage_edit', {'id': stage.id}) }}" class="btn-warning mt-2 text-white" style="text-decoration:none;padding: 6px 23px; border-radius:20px;box-shadow: 0px 4px 13px #afa15f;" >edit</a>

    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}
